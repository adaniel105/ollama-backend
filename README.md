rag pipeline with poppler for png generation from pdfs, cohere for embeddings, qdrant for vector storage, and local models (hermes, gpt oss) as a quantized llm for fast chat.

TODO : swap out cohere for a local model to gen image embeds